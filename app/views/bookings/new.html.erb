<%= form_with(model: @booking, local: true) do |form| %>
  <%= form.label :flight_id %>
  <%= form.text_field :flight.id %>

  <%= form.label :user.id %>
  <%= form.text_field :user.id %>

  <%= form.submit "Book Flight" %>
<% end %>