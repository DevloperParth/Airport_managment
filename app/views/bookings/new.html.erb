<h1>New of Bookings</h1>

<%= form_with model: @booking do |form| %>
  <div>
    <%= form.label :title %><br>
    <%= form.text_field :@booking. %>
  </div>

  <div>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>